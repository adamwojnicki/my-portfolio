import React from "react";
import styles from "./Personal.module.scss";

export default () => {
  return (
    <section className={`content-section ${styles.personal}`} id="personal">
      personal
    </section>
  );
};
